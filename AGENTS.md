# 仓库协作约束

## 产品边界

- 本项目面向个人开源、本机运行，不重新引入服务端校验、计费、API 密钥、数据库或商业发布链。
- 可交付能力默认使用 Python 标准 HTTP/协议实现；确需执行上游 JavaScript 时使用本机 Node.js/V8。不得把 Playwright、CDP、页面点击、页面导航或浏览器流程自动化作为产品运行时。
- 核心运行时保持 Python，不重新引入 Go 运行依赖。Node.js 只承载已提纯的本地签名、协议计算和扩展合同测试，不承载网页操作流程。
- Chrome 扩展是尚未完成协议提纯时的临时、被动会话桥：Cookie 和页面签名留在 Chrome，只允许查询已有匹配标签页并在页面上下文发请求。
- 扩展和 Python 守护进程不得创建、刷新、导航、聚焦或轮询等待平台标签页与页面运行时；缺页、未登录或运行时未就绪时立即返回明确错误，由使用者在浏览器中自行处理。
- 允许显式的交互式登录交接：使用者调用 `session login`，或交互式平台命令收到上述会话错误时，Python CLI 可通过操作系统在 Chrome 中打开一次白名单业务 URL，并有界等待扩展返回脱敏会话状态；就绪后只重放原请求一次。此流程不得点击页面、填写表单、提取 Cookie，也不得在非交互模式隐式启动。
- 新能力先尝试 Python 直连，其次提取为 Node/V8 本地签名；只有两者都缺少可重复证据时才保留浏览器桥，并在命令说明中明确标记为临时传输。

## 语言与文档

- 代码注释、docstring、仓库文档、生成的 Skill 和扩展可见文案使用中文。
- 命令名、参数名、协议字段、错误码、URL 和平台专有名词保持原值。
- 各平台 `cli.py` 的 `argparse` 声明是接口唯一事实来源；`skill/SKILL.md` 必须由代码生成。
- 不维护平台手写接口文档。架构和迁移边界只写入根 `README.md`，过时文档直接删除。

## 设计与实现

- 每个 `reverse/<platform>_reverse/` 是独立限界上下文；共享层只承载真实的跨平台能力。
- 优先标准库、现有依赖和删除式改造，不为单一实现增加仓储、工厂或预留接口。
- 同一平台族的浏览器请求必须串行，不同平台族必须可并行，禁止增加全局业务锁。
- 多扩展连接采用确定性粘性选择和断线退化；当前未暴露人工选择入口，但重复连接或额外实例不得导致守护进程崩溃。
- 可观测指标保存在内存并按需读取，不做高频磁盘写入；Popup 只展示能指导诊断的关键指标。

## App 扩展

- App Hook、版本绑定样本信息、移动身份结构和验证证据属于研究资产，不等同于可调用接口。
- 未经明确迁移决定，不删除已有 App Hook 或移动协议证据。
- 新 App 接口放入独立平台上下文；只有完成真实安装态、非空响应、错误分类和可重复测试后，才进入命令目录。
- 移动身份只保存在本机私有文件中，不与浏览器 Cookie、Web 签名状态混用。

## 验证

- 修改 Python、扩展或生成文档后运行 `make test PYTHON=.venv/bin/python`。
- 修改接口声明后运行 `make docs PYTHON=.venv/bin/python`，并确保 `make docs-check` 通过。
- 工作树可能包含用户尚未提交的改动；不得使用 reset、checkout 等方式覆盖这些改动。

## 目录级规则与测试

- `AGENTS.md` 按目录递归生效：根文件约束全仓库；更深目录中的文件只补充或收窄其作用范围，越靠近目标文件的规则优先。
- `reverse/<platform>_reverse/AGENTS.md` 用中文记录该平台可复现的研究记忆、已知坑、解决方式和证据边界；它不能把研究资产伪装成稳定接口。
- Python 单元测试、测试夹具和跨平台合同测试统一放在根目录 `test/`，分别使用 `test/<platform>/` 和 `test/runtime/`；生产包 `reverse/` 不再放测试源码或夹具。
- 新增平台测试必须建立独立的 `test/<platform>/` 包并保持 `__init__.py`；平台 fixture 不得包含真实 Cookie、令牌、代理凭据或本机私有身份。
